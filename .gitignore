# Secrets — never commit
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/

# Run logs — written locally and in CI but not tracked in git
# (the workflow commits state/seen_listings.json instead)
logs/*.log

# Generated reports — delivered via email; uploaded as artifacts on failure
out/*.csv
